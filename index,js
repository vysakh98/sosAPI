const Sequelize = require('sequelize');
const connection=require('../db/connection.js')
const other=require('../models/Othercontributions.js')

other.findAll({})
	.then(result=>{
		console.log(result)})

	.catch(err=>{
	console.log(err)
	})